# Deprecated: use environment-specific templates instead.
#
#   cp .env.dev.example  .env.dev   # development stack (port 8000 / 5432)
#   cp .env.prod.example .env.prod  # production-like stack (port 8001 / 5433)
